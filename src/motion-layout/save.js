// Motion Layout is a configuration block — clicking "Insert layout"
// transforms it into a Group of Columns/Column blocks. The Motion
// Layout block itself never persists, so save() returns null.
export default function save() {
	return null;
}